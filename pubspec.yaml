name: app_facul
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.6
  http: ^1.2.1
  shared_preferences: ^2.2.3
  socket_io_client: ^2.0.3+1
  provider: ^6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/imgs/logo-unicv.png"
  min_sdk_android: 21 
 
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/imgs/logo-unicv.png
    - assets/imgs/estudantes.jpg
    - assets/imgs/fachada.jpg
    - assets/imgs/logo-unicc.png

